@branchleft:registry=https://npm.pkg.github.com

# pnpm 11 holds back freshly published versions by default. Our own packages are
# published from CI we control, so the cooldown only ever breaks
# --frozen-lockfile right after a release.
minimumReleaseAgeExclude[]=@branchleft/*
