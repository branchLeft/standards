name: Docs lint

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  docs-lint:
    timeout-minutes: 5
    uses: branchLeft/github-workflows/.github/workflows/docs-lint.yml@v1.0.6
